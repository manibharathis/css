there are 6 header tags
iframe used to embed another website in our website
we use table inside forms toake it looks formatter
input tags are same only type will change
types -- text,select, submit,reset,file,radio
if therea are two radio button give name field to select any one
submit will change the url atlast with ?

each column in table must be inside row
anchored tag have href
br for nre line
